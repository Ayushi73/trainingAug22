#include<stdio.h>
#include<stdlib.h>
#define MAX 10

int main(){

int arr[MAX];

printf("\nEnter %d integers : \n", MAX);

// input the array elements
for(int i=0 ; i < MAX ; i++)
	{
	scanf("%d", &arr[i]);
	}





return EXIT_SUCCESS;
}
